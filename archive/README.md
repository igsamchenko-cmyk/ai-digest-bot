# Archive

This folder contains earlier experimental implementations of the bot.

The production GitHub Actions workflow runs `digest.py` from the repository root.
Keep archived files here for reference only, unless you intentionally decide to
restore one of them.
